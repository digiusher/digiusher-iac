# Scenario 2: EA With Enrollment Account
#
# Use this when:
# - Enterprise Agreement enrollment
# - HAS dedicated enrollment account
# - Export contains ONLY subscriptions in enrollment account

# Subscription and tenant
subscription_id = "REPLACE_WITH_SUBSCRIPTION_ID"
tenant_id       = "REPLACE_WITH_TENANT_ID"

# EA configuration with enrollment account
billing_scope_level    = "enrollment_account"
billing_account_id     = "REPLACE_WITH_ENROLLMENT_NUMBER"
enrollment_account_id  = "REPLACE_WITH_ENROLLMENT_ACCOUNT_ID"

# Storage (leave empty for auto-generated name)
storage_account_name   = ""
storage_container_name = "digiusher-focus-exports"

# Enable FOCUS exports
enable_cost_exports = true

# Optional: VM power scheduler
enable_power_scheduler = false

# Optional: Target specific subscriptions for Reader role
# Leave empty to target all subscriptions in enrollment account
target_subscription_ids = []
